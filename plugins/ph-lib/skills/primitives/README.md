# Primitives

**Membership test:** another plugin's router references this skill, so `ph-build`
and `ph-plan` are broken without it. That is what makes `ph-lib` a hard
dependency rather than a convenience.

All four are model-invocable.

- `codebase-design`
- `technical-writing`
- `unslop`
- `writing-for-agents`

Descriptions live in the [plugin README](../../README.md) — one place, so they
cannot drift.
